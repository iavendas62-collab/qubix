# GPU Matching Flow Diagram

## High-Level Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     Job Requirements Input                       │
│  • Job Type (MNIST, Stable Diffusion, etc.)                     │
│  • Framework (PyTorch, TensorFlow, JAX)                         │
│  • Estimated VRAM, RAM, Compute                                 │
│  • Optional Parameters (epochs, resolution, dataset size)       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Fetch Available Providers                       │
│  • Query database for online & available GPUs                   │
│  • Include GPU specs (model, VRAM, RAM, price)                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              For Each Provider: Calculate Metrics                │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Compatibility Check                                    │  │
│  │    • VRAM Ratio = GPU VRAM / Required VRAM               │  │
│  │    • RAM Ratio = GPU RAM / Required RAM                  │  │
│  │    • Classify: recommended/compatible/borderline/insuff  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 2. Duration Estimation                                    │  │
│  │    • Lookup benchmark for GPU model + job type           │  │
│  │    • Adjust for epochs (linear)                          │  │
│  │    • Adjust for resolution (quadratic)                   │  │
│  │    • Adjust for dataset size (linear)                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 3. Cost Calculation                                       │  │
│  │    • Cost = (Duration / 3600) × Price Per Hour           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 4. Cost-Benefit Score                                     │  │
│  │    • Performance Score = 1000 / Duration                 │  │
│  │    • Cost Score = 1 / (Cost + 0.01)                      │  │
│  │    • Final = (Perf × 0.6) + (Cost × 0.4) × 10           │  │
│  │    • Normalized to 0-100                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Sort & Filter Results                       │
│                                                                   │
│  Sort Options:                                                   │
│  • cost_benefit (default) - Highest score first                 │
│  • price_low - Lowest cost first                                │
│  • performance - Fastest duration first                         │
│  • availability - Online providers first                        │
│                                                                   │
│  Filter:                                                         │
│  • Remove insufficient GPUs from recommendations                │
│  • Keep top 3 for recommendations                               │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Return Results                            │
│                                                                   │
│  {                                                               │
│    compatibleGPUs: [                                            │
│      {                                                           │
│        provider: { id, gpuModel, vram, price, ... },           │
│        compatibility: "recommended",                            │
│        estimatedDuration: 120,                                  │
│        estimatedDurationFormatted: "2 minutes",                 │
│        estimatedCost: 0.067,                                    │
│        costBenefitScore: 85,                                    │
│        warnings: []                                             │
│      },                                                          │
│      ...                                                         │
│    ],                                                            │
│    recommendations: [...top 3...],                              │
│    totalAvailable: 10,                                          │
│    totalCompatible: 8                                           │
│  }                                                               │
└─────────────────────────────────────────────────────────────────┘
```

## Compatibility Classification

```
┌─────────────────────────────────────────────────────────────────┐
│                    Compatibility Tiers                           │
└─────────────────────────────────────────────────────────────────┘

VRAM Ratio = GPU VRAM / Required VRAM
RAM Ratio = GPU RAM / Required RAM

┌──────────────┬──────────────┬──────────────┬──────────────────┐
│ VRAM Ratio   │ RAM Ratio    │ Result       │ Badge Color      │
├──────────────┼──────────────┼──────────────┼──────────────────┤
│ ≥ 1.5        │ ≥ 1.3        │ Recommended  │ 🟢 Green         │
│ ≥ 1.2        │ ≥ 1.1        │ Compatible   │ 🟢 Green         │
│ ≥ 1.0        │ ≥ 1.0        │ Borderline   │ 🟡 Yellow        │
│ < 1.0        │ < 1.0        │ Insufficient │ 🔴 Red (Disabled)│
└──────────────┴──────────────┴──────────────┴──────────────────┘

Example:
  Job requires: 4GB VRAM, 8GB RAM
  GPU has: 8GB VRAM, 16GB RAM
  
  VRAM Ratio = 8 / 4 = 2.0 (≥ 1.5) ✓
  RAM Ratio = 16 / 8 = 2.0 (≥ 1.3) ✓
  
  Result: RECOMMENDED 🟢
```

## Duration Estimation with Parameters

```
┌─────────────────────────────────────────────────────────────────┐
│              Parameter-Based Adjustments                         │
└─────────────────────────────────────────────────────────────────┘

Base Benchmark: RTX 4090 + MNIST = 120s (5 epochs, 10k samples)

┌──────────────────────────────────────────────────────────────────┐
│ Parameter    │ Adjustment Formula        │ Example              │
├──────────────┼───────────────────────────┼──────────────────────┤
│ Epochs       │ Linear Scaling            │ 10 epochs = 120s × 2 │
│              │ Time × (new/base)         │ = 240s               │
├──────────────┼───────────────────────────┼──────────────────────┤
│ Resolution   │ Quadratic Scaling         │ 1024px = 300s × 4    │
│              │ Time × (new/base)²        │ = 1200s              │
├──────────────┼───────────────────────────┼──────────────────────┤
│ Dataset Size │ Linear Scaling            │ 20k = 120s × 2       │
│              │ Time × (new/base)         │ = 240s               │
└──────────────┴───────────────────────────┴──────────────────────┘

Combined Example:
  Base: 120s (5 epochs, 512px, 10k samples)
  New: 10 epochs, 1024px, 20k samples
  
  Epoch adjustment: 120s × (10/5) = 240s
  Resolution adjustment: 240s × (1024/512)² = 960s
  Dataset adjustment: 960s × (20000/10000) = 1920s
  
  Final estimate: 1920s = 32 minutes
```

## Cost-Benefit Scoring

```
┌─────────────────────────────────────────────────────────────────┐
│                  Cost-Benefit Algorithm                          │
└─────────────────────────────────────────────────────────────────┘

Step 1: Calculate Performance Score
  Performance = 1000 / Duration (seconds)
  Higher is better (faster = higher score)

Step 2: Calculate Cost Score
  Cost Score = 1 / (Cost + 0.01)
  Higher is better (cheaper = higher score)

Step 3: Weighted Combination
  Raw Score = (Performance × 0.6) + (Cost Score × 0.4)
  
Step 4: Normalize to 0-100
  Final Score = min(100, Raw Score × 10)

Example:
  GPU A: 120s duration, $0.50 cost
    Performance = 1000/120 = 8.33
    Cost Score = 1/0.51 = 1.96
    Raw = (8.33 × 0.6) + (1.96 × 0.4) = 5.78
    Final = 57.8 → 58/100
  
  GPU B: 180s duration, $0.30 cost
    Performance = 1000/180 = 5.56
    Cost Score = 1/0.31 = 3.23
    Raw = (5.56 × 0.6) + (3.23 × 0.4) = 4.63
    Final = 46.3 → 46/100
  
  Result: GPU A wins (58 > 46) - faster is worth the extra cost
```

## Sorting Strategies

```
┌─────────────────────────────────────────────────────────────────┐
│                     Sorting Options                              │
└─────────────────────────────────────────────────────────────────┘

1. Cost-Benefit (Default)
   Sort by: costBenefitScore (descending)
   Best for: Balanced value - good performance at fair price
   
   Example Order:
   1. RTX 3060: Score 65 (slow but very cheap)
   2. RTX 3080: Score 58 (fast and reasonable)
   3. RTX 4090: Score 45 (fastest but expensive)

2. Price Low
   Sort by: estimatedCost (ascending)
   Best for: Budget-conscious users
   
   Example Order:
   1. RTX 3060: $0.25
   2. RTX 3070: $0.35
   3. RTX 3080: $0.50

3. Performance
   Sort by: estimatedDuration (ascending)
   Best for: Time-sensitive jobs
   
   Example Order:
   1. RTX 4090: 120s
   2. RTX 3090: 180s
   3. RTX 3080: 220s

4. Availability
   Sort by: isOnline && isAvailable (descending)
   Best for: Immediate job execution
   
   Example Order:
   1. Online & Available GPUs
   2. Online but Busy GPUs
   3. Offline GPUs
```

## Integration with Frontend

```
┌─────────────────────────────────────────────────────────────────┐
│                  Frontend Component Flow                         │
└─────────────────────────────────────────────────────────────────┘

JobUploader (Task 2)
    │
    │ Analyzes file
    │ Extracts requirements
    ▼
SmartMatcher (Task 4) ◄─── Calls GPU Matching API
    │
    │ Displays compatible GPUs
    │ Shows recommendations
    │ Allows sorting
    ▼
JobWizard Step 2 (Task 5)
    │
    │ User selects GPU
    ▼
Job Launch with Escrow (Task 6-8)
```

## Performance Characteristics

```
┌─────────────────────────────────────────────────────────────────┐
│                    Performance Metrics                           │
└─────────────────────────────────────────────────────────────────┘

Time Complexity:
  • Benchmark lookup: O(1) - constant time hash lookup
  • Compatibility check: O(1) per GPU
  • Duration estimation: O(1) per GPU
  • Cost calculation: O(1) per GPU
  • Sorting: O(n log n) where n = number of GPUs
  • Overall: O(n log n) dominated by sorting

Space Complexity:
  • Benchmarks: O(1) - fixed 30 entries
  • Results array: O(n) where n = number of GPUs
  • Overall: O(n)

Typical Performance:
  • 10 GPUs: < 5ms
  • 100 GPUs: < 50ms
  • 1000 GPUs: < 500ms
```
